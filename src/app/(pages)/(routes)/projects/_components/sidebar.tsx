import React from 'react'

interface Props {
    projectId: string;
}

export const Sidebar = ({ projectId }: Props) => {
    return (
        <div className='w-full'>
            projects
        </div>
    )
}
